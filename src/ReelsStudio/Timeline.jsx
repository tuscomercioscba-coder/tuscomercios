import {formatTime} from "./utils";export default function Timeline({clips,duration,currentTime,selectedClipId,disabled,onSeek,onSelect,onMoveLeft,onMoveRight}){if(!duration||!clips.length)return <section className="rounded-[2rem] border border-slate-200 bg-white p-5 shadow-xl"><h3 className="text-lg font-black text-slate-950">Línea de tiempo</h3><p className="mt-2 text-sm font-semibold text-slate-500">Cuando subas un video aparecerá aquí completo.</p></section>;const cursor=Math.max(0,Math.min(100,currentTime/duration*100));return <section className="rounded-[2rem] border border-slate-200 bg-white p-4 shadow-xl sm:p-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><h3 className="text-lg font-black text-slate-950">Línea de tiempo</h3><p className="mt-1 text-sm font-semibold text-slate-500">Reproducí el video y cortá exactamente donde estás mirando.</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-black text-blue-700">{formatTime(duration)}</span></div><div className="relative mt-5 overflow-hidden rounded-2xl bg-slate-900 p-3"><div className="absolute bottom-2 top-2 z-20 w-0.5 bg-red-500" style={{left:`${cursor}%`}}/><div className="flex min-h-28 gap-2 overflow-x-auto pb-2">{clips.map(clip=>{const d=clip.end-clip.start,w=Math.max(110,d/duration*900),sel=selectedClipId===clip.id;return <button key={clip.id} disabled={disabled} onClick={()=>{onSelect(clip.id);onSeek(clip.start)}} className={`relative shrink-0 overflow-hidden rounded-xl border-2 p-3 text-left transition disabled:opacity-40 ${sel?"border-blue-400 bg-blue-600 text-white":"border-slate-600 bg-slate-800 text-white"}`} style={{width:w}}><span className="block text-xs font-black uppercase tracking-wide opacity-70">{clip.name}</span><span className="mt-3 block text-base font-black">{formatTime(clip.start)} → {formatTime(clip.end)}</span><span className="mt-1 block text-xs font-bold opacity-70">{d.toFixed(1)} segundos</span><div className="mt-3 grid grid-cols-2 gap-2"><span role="button" tabIndex={0} onClick={e=>{e.stopPropagation();onMoveLeft(clip.id)}} className="flex min-h-9 items-center justify-center rounded-lg bg-black/25 text-xs font-black">← Antes</span><span role="button" tabIndex={0} onClick={e=>{e.stopPropagation();onMoveRight(clip.id)}} className="flex min-h-9 items-center justify-center rounded-lg bg-black/25 text-xs font-black">Después →</span></div></button>})}</div><input type="range" min={0} max={duration} step={0.01} value={currentTime} disabled={disabled} onChange={e=>onSeek(Number(e.target.value))} className="relative z-30 mt-2 w-full accent-red-500"/></div></section>}
