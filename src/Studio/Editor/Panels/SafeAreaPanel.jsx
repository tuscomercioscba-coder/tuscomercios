import { SAFE_MARGIN_PRESETS } from "../Utils/constants";
export default function SafeAreaPanel({visible,margin,onVisible,onMargin}){return <section className="rounded-[1.5rem] border border-white bg-white p-4 shadow-lg"><div className="flex items-center justify-between gap-3"><div><h3 className="text-lg font-black text-slate-950">Márgenes seguros</h3><p className="mt-1 text-sm font-semibold text-slate-500">Evitan textos y logos pegados al borde.</p></div><input type="checkbox" checked={visible} onChange={e=>onVisible(e.target.checked)}/></div>{visible&&<div className="mt-4 grid grid-cols-2 gap-2">{SAFE_MARGIN_PRESETS.map(p=><button key={p.id} onClick={()=>onMargin(p.value)} className={`rounded-xl border p-3 text-sm font-black ${margin===p.value?"border-amber-500 bg-amber-50 text-amber-700":"border-slate-200 bg-white text-slate-700"}`}>{p.label}</button>)}</div>}</section>}
