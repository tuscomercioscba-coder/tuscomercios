import { ICON_LIBRARY, STICKER_LIBRARY } from "../Utils/constants";
export default function ElementsLibraryPanel({onAddIcon,onAddSticker,onAddLine}){return <section className="rounded-[1.5rem] border border-white bg-white p-4 shadow-lg"><h3 className="text-lg font-black text-slate-950">Íconos y elementos</h3><p className="mt-1 text-sm font-semibold text-slate-500">Elementos rápidos para promociones y publicaciones.</p><h4 className="mt-5 text-sm font-black text-slate-800">Íconos</h4><div className="mt-3 grid grid-cols-4 gap-2">{ICON_LIBRARY.map(i=><button key={i.id} onClick={()=>onAddIcon(i)} className="flex aspect-square items-center justify-center rounded-xl border border-slate-200 bg-slate-50 text-3xl" title={i.label}>{i.symbol}</button>)}</div><h4 className="mt-5 text-sm font-black text-slate-800">Stickers</h4><div className="mt-3 grid grid-cols-2 gap-2">{STICKER_LIBRARY.map(s=><button key={s.id} onClick={()=>onAddSticker(s)} className="min-h-12 rounded-xl px-3 text-sm font-black shadow-sm" style={{background:s.fill,color:s.color}}>{s.label}</button>)}</div><button onClick={onAddLine} className="mt-4 min-h-12 w-full rounded-xl bg-slate-900 font-black text-white">Agregar línea</button></section>}
